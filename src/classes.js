import React from 'react';

function Classes(props){
    return(
        <div>
            <h1>We are starting classes {props.classesName}</h1>
            <h1>which are starting from {props.startDay}</h1>
        </div>
    )
}
export default Classes;